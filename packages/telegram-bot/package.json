{
  "name": "@dashboard/telegram-bot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dashboard/shared": "workspace:*",
    "grammy": "^1.31.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/bun": "^1.2.0"
  }
}
